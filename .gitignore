node_modules
.next
*.tsbuildinfo
.env
.env.local
.env*.local
.env.vercel
# Credenciales de la cuenta de servicio de GA4 (secreto, jamás al repo)
ga4-sa.json
screenshots/
datosNegoios/

.vercel
.claude/scheduled_tasks.lock

# Fichas investigadas por el agente (transitorias, se ingestan y se descartan)
tmp/

# Refs de diseño (PNGs pesados, input de Claude design) — no al historial
design_briefs/*_ref/
